description: OpenTelemetry tracing and metrics for loadshift
product: jvm/lib

apply:
  - ../templates/library.module-template.yaml

dependencies:
  - ../loadshift-core
  - $libs.kotlinx.coroutines.core
  - $libs.opentelemetry.api

test-dependencies:
  - $libs.kotlinx.coroutines.test
  - $libs.opentelemetry.sdk
  - $libs.opentelemetry.sdk.testing
